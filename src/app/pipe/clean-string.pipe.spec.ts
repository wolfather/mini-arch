import { CleanStringPipe } from './clean-string.pipe';

describe('CleanStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanStringPipe();
    expect(pipe).toBeTruthy();
  });
});
